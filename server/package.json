{
  "type": "module",
  "scripts": {
    "dev": "tsx index.ts",
    "build": "esbuild index.ts --bundle --platform=node --outdir=dist --format=esm",
    "start": "NODE_ENV=production node dist/index.js"
  }
}
